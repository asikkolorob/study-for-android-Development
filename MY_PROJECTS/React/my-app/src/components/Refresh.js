import React from 'react';

const Refresh = () => {

    return (
        <div>
            <button> Refresh Button </button>
            <h1>{ Math.random() }</h1>
        </div>
    );
}

export default Refresh;

